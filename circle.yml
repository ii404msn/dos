machine:
  pre:
    - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.6 10
    - sudo apt-get install libreadline-dev
test:
  pre:
    - sh build.sh && cd sandbox && sh start_all.sh

